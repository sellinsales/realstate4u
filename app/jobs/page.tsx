export default function JobsPage() {
  return (
    <main className="section-spacing">
      <div className="page-shell">
        <div className="panel rounded-[2rem] p-8">
          <span className="eyebrow">Phase 4 placeholder</span>
          <h1 className="mt-5 text-5xl font-semibold text-[var(--brand-blue)]">Construction jobs will live here later.</h1>
          <p className="mt-4 max-w-2xl text-base leading-8 text-[var(--muted)]">
            The job board is intentionally deferred while the property and queue housing flows are being hardened.
          </p>
        </div>
      </div>
    </main>
  );
}
