export default function ServicesPage() {
  return (
    <main className="section-spacing">
      <div className="page-shell">
        <div className="panel rounded-[2rem] p-8">
          <span className="eyebrow">Phase 4 placeholder</span>
          <h1 className="mt-5 text-5xl font-semibold text-[var(--brand-blue)]">Home services will live here later.</h1>
          <p className="mt-4 max-w-2xl text-base leading-8 text-[var(--muted)]">
            The route exists now so the platform keeps a clean multi-vertical structure without prematurely building the service marketplace.
          </p>
        </div>
      </div>
    </main>
  );
}
